apiVersion: v1
kind: Service
metadata:
  name: artifact-registry
  namespace: artifact-registry
  labels:
    app: artifact-registry
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: artifact-registry
